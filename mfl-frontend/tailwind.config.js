/** @type {import('tailwindcss').Config} */
export default {
    // content 字段在 v4 中是自动处理的，通常不需要写
    theme: {
        extend: {
            // 在这里添加您的自定义配置
            // 例如:
            // colors: {
            //   'custom-blue': '#243c5a',
            // }
        },
    },
    plugins: [],
}